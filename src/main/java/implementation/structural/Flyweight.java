package implementation.structural;

/**
 * Flyweight provides virtual instances of a class
 * By sharing common parts of state between multiple objects instead of keeping all of the data in each object, common parts will be managed by a single instance
 */
public class Flyweight {
}
